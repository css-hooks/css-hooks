```tsx
import hooks from "@css-hooks/solid";

function Component() {
  return (
    <button
      style={hooks({
        color: "black",
        hover: {
          color: "green",
        },
      })}
    >
      Hello hooks!
    </button>
  );
}
```
