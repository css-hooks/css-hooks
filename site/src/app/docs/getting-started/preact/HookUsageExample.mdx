```tsx
import hooks from "@hooks.css/preact";

function Component() {
  return (
    <button
      className="hooks"
      style={hooks({
        color: "black",
        hover: {
          color: "green",
        },
      })}
    >
      Hello hooks!
    </button>
  );
}
```
